# Blockers for MagnetarHeimarmene

| ID | Description | Creation Date | Owner | Status |
| :--- | :--- | :--- | :--- | :--- |
| blocker-test-debug-qmltestrunner | `test.yml` debug step checks `qml` instead of `qmltestrunner`; keep `test-v2.yml` while validating debug/runtime paths. Must test `qmltestrunner` availability before changing. | 2026-01-21 | Jules | Active |

## Escalation Process
If a blocker remains `Active` for more than **1 business day**:
1.  Raise visibility in `STATUS.md`.
2.  Consult the project owner/maintainer.
